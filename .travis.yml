notifications:
  email: false
language: ruby
rvm:
  - 2.6.2
cache:
  bundler: true
  directories:
    - vendor/bundle
    - $HOME/.rvm/gems
before_script:
  - gem install bundler:2.0
  - bundle install
script:
  - bundle exec rubocop
